import { BasePersistentFields } from './base-persistent-fields';

describe('BasePersistentFields', () => {
  it('should create an instance', () => {
    expect(new BasePersistentFields()).toBeTruthy();
  });
});
