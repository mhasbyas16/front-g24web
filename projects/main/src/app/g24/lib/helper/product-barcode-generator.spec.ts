import { ProductBarcodeGenerator } from './product-barcode-generator';

describe('ProductBarcodeGenerator', () => {
  it('should create an instance', () => {
    expect(new ProductBarcodeGenerator()).toBeTruthy();
  });
});
