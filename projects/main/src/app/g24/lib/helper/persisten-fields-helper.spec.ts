import { PersistenFieldsHelper } from './persisten-fields-helper';

describe('PersistenFieldsHelper', () => {
  it('should create an instance', () => {
    expect(new PersistenFieldsHelper()).toBeTruthy();
  });
});
